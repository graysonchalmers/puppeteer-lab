# 2026-09-16 (Task 2) - Worker serialization + chunked fallback (TDD-002 P2, item 6)

_Migrated verbatim from the inline HANDOFF.md session log on 2026-09-22._

- Continuing from Task 1 (`9652985`), plan `docs/superpowers/plans/2026-09-16-item6-schema-v3-blender-importer.md`. New `components/shared/serialize.worker.ts` imports Task 1's `buildEnvelope`/`buildKinematics`/`serializeV3` unchanged and posts back a `Blob`. `hooks/useRecorder.ts`'s `exportData` is now `async`: tries the worker first (lazily constructed, never at module scope), falls back to a new `serializeV3Chunked` in `recordingSchema.ts` (chunks the frame-mapping loop, yields via `setTimeout(0)` every 500 frames, reuses the same private per-frame mappers) when `typeof Worker === 'undefined'`, construction throws, or the worker errors/fails to load. No caller changes needed (`RecorderControls.tsx` and the three demo components already just call `exportData` fire-and-forget).
- Two design calls the brief left open, made and recorded in the plan + `recordingSchema.ts` comments: (1) the worker/fallback request shape gained an optional `audio?: RecordingV3Audio` field beyond the brief's literal `{frames,type,kind,durationMs,video?}`, since `parseDataUrl` stays synchronous on the main thread per the brief but its result still has to reach the envelope before the one `serializeV3` call; (2) `SerializeResponse` uses a string-literal `status: 'ok' | 'error'` discriminant instead of `ok: true | false` after discovering, via an isolated repro, that this repo's local TS 5.8.3 does not narrow a boolean-literal discriminated union on `if (x.ok)` (every member stays visible, the "wrong" branch's field errors as missing) while a string-literal discriminant narrows correctly.
- 8 new tests in `recordingSchema.test.ts` for `serializeV3Chunked`: matches the synchronous builders (ignoring `createdAt`), audio attaches only on `'full'`, `capture.fps`/`frameCount` computed against the whole take (not a chunk slice) at the exact 500-frame boundary and again at 1200 frames, a `setTimeout` spy confirms exactly two yields for 1200 frames and zero for an empty take.
- Gate green: typecheck clean, 62/62 tests, `npm run build` emits `dist/assets/serialize.worker-*.js` (4.11 kB) as its own chunk, `npm run smoke` passes (14 JS assets, no secret leak, vendored assets + stamp present).
- **Not verified this session (host-only, same camera-preview gap as items 1-4):** the actual "no long task over 100 ms" DevTools Performance claim. Recorded as 🔌 wired in the plan and item 7 of this file's host-verification checklist above.
- Commit: worker + fallback + wiring + tests.
